<div class="post-item">
    <%= link_to post.title, post_path(post), class: "post-item__title" %>
    <p class="post-item__body"><%= limit_text(post.body) %></p>
    <p class="post-item__date"><%= pretty_date(post.created_at) %></p>
</div>
